{% extends 'base.html' %}

{% block header %}
<h1>{% block title %}Most similar lemmata for word list "{{ word_list['title'] }}"{% endblock %}</h1>
{% endblock %}

{% block content %}
<article class="post">
  <header>
    <div>
      <h1>{{ word_list['title'] }}</h1>
      <div class="about">by {{ word_list['username'] }} on {{ word_list['created'].strftime('%Y-%m-%d') }}</div>
    </div>
    {% if g.user['id'] == word_list['author_id'] %}
    <a class="action" href="{{ url_for('word_list.update', id=word_list['id']) }}">Edit</a>
    {% endif %}
  </header>
  <p class="body">{{ word_list['lemmata'] }}</p>
  <p class="body">{{ similar_ones }}</p>
</article>
<hr>
{% endblock %}
